# Design Up

Static landing page deployed on Cloudflare Pages.

## Local preview

```bash
npm install
npm run dev
```

## Deploy

```bash
npm run deploy
```

The deployable site is in `site/`, with fonts, images, and video stored under `site/assets/`.
